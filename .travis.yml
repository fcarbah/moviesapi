sudo: required
services:
    - docker

script:
    echo "no tests setup"

after_success:
    docker build -t fcarbah/moviesapi .


- echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin

- docker push fcarbah/moviesapi